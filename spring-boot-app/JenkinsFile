pipeline {
    agent any

    tools {
        // This must match the Maven installation name in Manage Jenkins → Tools
        maven 'Maven'
    }

    environment {
        DOCKER_IMAGE = 'babu120987/my-app:latest' // Replace with your DockerHub username
    }

    stages {

        stage('Checkout') {
            steps {
                // Optional: Jenkins declarative checkout will automatically happen
                echo 'Checking out code...'
            }
        }

        stage('Build & Test') {
            steps {
                dir('spring-boot-app') {
                    sh 'mvn clean package'
                }
            }
        }

        stage('Sonar Analysis') {
            steps {
